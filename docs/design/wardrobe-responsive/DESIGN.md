# Responsive Noot wardrobe — 2026-09-08

## Truth and preserved behavior
User reports cramped mobile controls, unused large-monitor space, and confusing customization. Current mobile capture at 390×844 shows 10–12px item/option text, a horizontal item strip with hidden choices, color/pattern/preset controls sharing one short scroll area, and four vertical category tabs competing with the preview. Desktop currently caps the entire dialog at 960px and the model at 350px. Preserve the real cached Blender/Three rig, gesture/angle controls, all 28 items, nine looks, 22 swatches/custom color, separate material colors/patterns, local persistence, name validation, dialog focus/escape/close and theme. No new model, storage, or audio behavior.

Task: enter wardrobe → choose body category → browse/equip a clearly named item → optionally edit its colors → inspect front/turn/back → return to game. Whole looks are a peer category, not a hidden strip beneath every editor. Name edits remain available beside Done, outfit saving stays automatic and truthfully reported.

## Alternatives before product changes
A. Fitting room: vertical category rail / large character / narrow item menu on desktop. On phones, preview above a horizontal category rail and a bounded scrolling menu; colors replace the item list with a Back control. Advantage: one decision at a time with preview always visible. Cost: one extra action to edit colors.
B. Bottom workbench: full-width character, category strip, horizontal inventory below with inline colors. Advantage: broad item comparison; cost: reproduces sideways browsing and crowds short phone heights.
C. Catalog first: left item list, smaller right preview, expandable color rows. On mobile one scroll column with the model at top. Advantage: many items visible; cost: preview scrolls away while editing, original key feedback is lost.
Provisional A. Same real labels, selection, fabric color and angle action in all prototypes. 8px spacing with 4px refinements; 44px minimum targets; 14px controls; serif title 32–40px and sans section heading 20–24px. Desktop width up to 1360px, height up to 860px; inspector around 380–440px, preview gets remaining area. Mobile full-screen sheet, safe-area footer, no sideways item browsing. Short landscape follows compact desktop geometry when width permits.

## Visual and interaction contract
Keep Open Runde and Instrument Serif from the game's installed fonts. Existing canvas, text, hairline and accent tokens govern light/dark. Noot is the primary visual; use a quiet inset preview surface and one selected-row tint rather than nested cards. Lucide icons already installed, custom item glyphs remain project-owned. No new external art. Local reference study otherkind-project-index suggests grouping through aligned rows rather than repeated enclosures; this is an inference applied to inventory, not user research or copied artwork.

Category changes reset scroll and color subview. List supports arrows/Home/End with selection kept in view. Category navigation supports both axes as orientation changes. Color Back restores selected item focus; native swatches and picker retain labels. Looks show names and a short clothing summary, selected state only when all preset fields match. No fake save action: Done saves the name; outfits persist as before. Fixed close, preview and footer; only item/color panel scrolls. Motion uses existing Noot interpolation, immediate control switching, no new spatial animation. Focus outlines and reduced motion preserved.

Wording: “Your wardrobe”, Head / Eyes / Outfit / Feet / Looks, “Color & pattern”, “Back to items”, “Saved on this device”, “Done”. No decorative flavor descriptions beside every item. Verification pending: before/after 390×844, 320×640, large 1920×1080; browse last item, colors/pattern, preset, angle, keyboard, name errors, reload, dark and model continuity. No subagents/fresh reviewers available; comparisons are self-review, user acceptance pending.

## Prototype comparison
Authored local wireframe → actual Open Runde/Instrument Serif type revision → token-like surface revision in /tmp/noot-side-check/layouts*.html. A/B/C desktop and C/B/A narrow captures inspected with working item/angle/color actions. A preserves a stable character and readable vertical menu. B's wrapping inventory loses consistent reading order. C's phone document pushes the preview away during long inventory browsing. Select A. Prototype deliberately used a labeled preview placeholder; product validation must confirm the existing live rig fits. Narrow prototype's category placeholder is vertically stacked and wastes space; implement five real horizontal buttons. Keep surface chrome minimal. These are self-comparisons, not independent user evaluations.

## Rendered implementation review
Selected fitting-room direction implemented in NootProfile and scoped wardrobe.css. The inspector is a vertical inventory; Color opens a dedicated material editor, and Looks is a fifth peer category. No item/model/storage schema changes. All existing items, looks and pattern/color choices retained. Name field is 16px to avoid the small-font mobile zoom trigger. Status explicitly says “Outfit saved on this device,” since Done commits name edits separately.

Observed repairs: inherited settings-sheet-head height:40px clipped the title after adding insets; explicitly reset height:auto. Inherited console profile align-items:start prevented full-height panels; explicitly stretch the grid. Resize now keeps the equipped row visible when crossing the phone breakpoint. Color entry and return restore keyboard focus. Landscape hides category captions for space, so explicit accessible names preserve the icon tabs' meaning.

Checks completed:
- Actual renders at 390×844, 320×640, 844×390 and 1920×1080. 320px sheet has exactly 320px width; footer y538/h90 inside 640px; list is independently scrollable with 209px of visible content. 1920px fitting room is 1360×860, with the real character filling its preview instead of remaining 350px tall.
- Whole-look selection, color change, item selection, front/turn/back, keyboard End to Bandana, category arrow navigation, color-view focus return, invalid one-character name recovery, Done/reopen with retained appearance. Actual game route entered from Settings → Customize your Noot and Escape restored focus to Open settings. The live rig remains outside conditional inventory panels.
- Light and dark reviewed. Actual app mobile run logged no errors. Temporary standalone fixture hot reload initially double-mounted its test root; refreshed the harness and verified the normal app separately. Not a production component error.
- Typecheck, scoped lint, production build and 42 profile/model/asset tests pass. Existing build-size warnings remain. No new animation or extra 3D canvases added. Reduced-motion CSS and existing model behavior preserved; no native iOS keyboard session or independent user test performed.

Visual self-review: clear category / item / optional color sequence; model, close and Done remain visible. Selected row has an outline and check as well as color. Large-screen preview area is occupied by the live character; list width is bounded for readable labels. Source model/fashion appearance is intentionally unchanged. Human taste acceptance pending.
